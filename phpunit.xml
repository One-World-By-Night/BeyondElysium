<?xml version="1.0"?>
<phpunit
    bootstrap="tests/bootstrap.php"
    colors="true"
    convertDeprecationsToExceptions="true"
    convertErrorsToExceptions="true"
    convertNoticesToExceptions="true"
    convertWarningsToExceptions="true"
    beStrictAboutTestsThatDoNotTestAnything="true"
    failOnWarning="true"
    failOnRisky="true">

    <!-- Three layers, see BE_PROCESS/TESTING.md.
         unit     - pure logic, no WordPress, no database
         thread   - one full path through the stack, needs WordPress + MariaDB
         workflow - multi-actor scenarios, needs WordPress + MariaDB -->
    <testsuites>
        <testsuite name="unit">
            <directory suffix="Test.php">tests/unit</directory>
        </testsuite>
        <testsuite name="thread">
            <directory suffix="Test.php">tests/thread</directory>
        </testsuite>
        <testsuite name="workflow">
            <directory suffix="Test.php">tests/workflow</directory>
        </testsuite>
    </testsuites>

    <coverage>
        <include>
            <directory suffix=".php">beyond-elysium/includes</directory>
        </include>
    </coverage>
</phpunit>
