# Build artifacts
node_modules/
build/
vendor/
dist/

# Generated by bin/dist (wp i18n make-pot, against freshly-built build/*.js so the template
# can't silently drift from the code, Step 11d workflow-0.9.md) - not source, not committed,
# same treatment as build/ above. Ships in the dist artifact, regenerated every time.
beyond-elysium/languages/*.pot

# OS files
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/

# Logs
*.log

# Test tooling
.phpunit.result.cache

# Reference material dropped into the working tree ad hoc (a saved third-party site capture,
# not authored here, not runtime data) - never tracked, never shipped.
pp-samples/

# The raw MET-Mechanics export this session imports from - reference material in its original
# form (data-samples/MET-Mechanics - Complete Dataset.csv). The runtime copy the plugin actually
# reads lives at data/met-mechanics.csv and IS tracked; this directory never is.
data-samples/

# Real credentials for live hosts (e.g. kony-sabbat.net's be-admin account) - never tracked,
# never shipped. See BE_PROCESS/_credentials.md.
BE_PROCESS/_credentials.md
